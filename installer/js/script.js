$(document).ready(function() {
    var Installer, Step, Group, Field;

    Installer = (function() {
        function Installer() {
            //Create the global manager
        }

        /*Installer.prototype.ma = function() {

        };*/

        return Installer;
    })();
    
    Step = (function() {
        function Step() {
            //Build the group list
        }
        
        return Step;
    })();
    
    Group = (function() {
        function Group() {
            //Build the field list
            //Stores url (command)
        }
        
        return Group;
    })();
    
    Field = (function() {
        function Field() {
            //
        }
        
        return Field;
    })();
});

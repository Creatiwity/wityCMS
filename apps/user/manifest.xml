<?xml version="1.0" encoding="utf-8"?>
<app>
	<!-- Application name -->
	<name>User</name>
	
	<version>0.1</version>
	
	<!-- Last update date -->
	<date>25-02-2013</date>
	
	<!-- Tiny icone to be displayed in the admin board -->
	<icone></icone>
	
	<!-- Permissions -->
	<permission name="add" />
	<permission name="edit" />
	<permission name="delete" />
	<permission name="groups" />
	
	<!-- Front actions -->
	<action default="default" alias="connexion">login</action>
	<action requires="connected" alias="deconnexion">logout</action>
	<action requires="not-connected">register</action>
	<action requires="not-connected">confirm</action>
	<action requires="not-connected" alias="password-lost">password_lost</action>
	
	<!-- Admin actions -->
	<admin>
		<action desc="Liste des membres" default="1">listing</action>
		<action desc="Ajouter un utilisateur" requires="add">add</action>
		<action desc="Éditer un utilisateur" menu="false" requires="edit">edit</action>
		<action desc="Supprimer un utilisateur" menu="false" requires="delete">del</action>
		<action desc="Gestion des groupes" requires="groups">Groups</action>
		<action desc="Suprimer un groupe" menu="false" requires="groups,delete">group_del</action>
		<action desc="" menu="false" requires="groups,edit">group_dif</action>
		<action desc="" menu="false" requires="groups,edit">load_users_with_letter</action>
	</admin>
</app>